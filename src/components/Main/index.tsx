import { List } from '../List';
import { Wrapper } from '../Wrapper';
import './styles.css';

export const Main = () => {

    return (
        <main>
            <Wrapper>
                <List />
            </Wrapper>
        </main>
    );
}